apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-service-indian-store
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-service-indian-store
  template:
    metadata:
      labels:
        app: mcp-service-indian-store
    spec:
      containers:
        - name: indian-store-mcp
          image: ghcr.io/vishalk17/indian-store-mcp-server:0.008
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: indian-store-config
            - secretRef:
                name: indian-store-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-service-indian-store
  namespace: default
spec:
  selector:
    app: mcp-service-indian-store
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
